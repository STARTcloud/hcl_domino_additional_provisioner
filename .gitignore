## Don't Save Secrets That pertain to Secure details
.secrets.yml
vault.yml
## Don't Save Log Files
*.log
support-bundle.zip
.claude
# Don't Include Vagrant support files
.vagrant
id_rsa
Hosts.yml
results.yml

# The core_provisioner driver is fetched at build/bootstrap from the release
# pinned in driver.version — never committed. Its ssls/ subtree is the
# canonical SSL location (synced to /secure/; BYO certs drop in there too).
driver/
DeployKey_rsa
*.vdi
vault-Docova.yml

# Don't include your ID Files!
*.IDS
*.ID
*.id
*.ids
SAFE.IDS
# Except org certifier SAFE copies (public cert only, no private key) —
# needed so new domains can cross certify the org out of the box
!id-files/org-safe-ids/*.id
!id-files/org-safe-ids/*.ids

# Build Files to Ignore
.DS_Store
*.swp
.classpath
.project
.vscode/
.settings/
.gradle/
build/
test_files/

# Installer binaries never get committed
installers/*/archives/*.tar.xz
installers/*/archives/*.tar.gz
installers/*/archives/*.tar
installers/*/archives/*.zip
installers/*/archives/*.gz
installers/*/archives/*.tgz
